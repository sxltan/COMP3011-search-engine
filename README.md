# COMP3011 Coursework 2 - Search Engine Tool

This project implements a simple search engine that:
- Crawls a website
- Builds an inverted index
- Supports search queries via a command-line interface

## Setup (so far)

```bash
pip install -r requirements.txt